/**
 * In javascript there is three ways to create variable
 * 1. using let
 * 2. using const
 * 3. using var
 *
 * 1. => when we need a variable that belongs to a specific only. we want that variable can be used outside of that block in that case we use
 *       let keyword.
 * 
 * 2. => When we need a variable that vaule should not be change anywhere in or file, in that case we use const keyword to create variable.
 * 
 * 3. => When variable created using var keyword that varialbe becomes a window object. hence it become a global variable also.
 * 
 *  
 */

// __2variables&Scope.js
 
